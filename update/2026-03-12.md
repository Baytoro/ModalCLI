# 2026-03-12

## 1) 新增 Tensor Core workload
- 改了什么：
  - 新增 `workloads/tensor_core/config.json`
  - 新增 `workloads/tensor_core/data.py`
  - 新增 `workloads/tensor_core/message.py`
  - 新增 `workloads/tensor_core/tensor_core.cpp`
  - 新增 `workloads/tensor_core/tensor_core_base.cu`
- 为什么改：需要基于方案 A（WMMA）测 Tensor Core 算力（FP16 输入 + FP32 累加）。
- 结果：`python cli.py tensor_core` 可运行并输出 `ms + tflops`。

## 2) 调整 Variants 输出格式
- 改了什么：`cli.py` 中 benchmark 输出由 `custom_ms=... ms` 改为 `... ms`；`key=value` 消息改为 `value key`。
- 为什么改：统一终端展示格式，符合 `base 4.075388 ms 32.93 tflops` 的目标样式。
- 结果：Variants 行为 `base         3.723072 ms 2379.32 tflops`。

## 3) 修复 Tensor Core 构建/加载错误
- 改了什么：`tensor_core_base.cu` 用 `kWarpSize=32` 替代 host 侧 `warpSize`。
- 为什么改：修复扩展加载报错 `undefined symbol: __device_builtin_variable_warpSize`。
- 结果：Modal 实跑通过。

## 4) 目录与入口统一
- 改了什么：
  - 目录从 `worloads/` 重命名为 `workloads/`
  - `cli.py` 支持简写：`python cli.py <workload_name>`
  - 保留 `-p/--path` 兼容与旧目录 fallback
- 为什么改：简化使用方式并修正目录命名。
- 结果：`python cli.py vector_add` 与 `python cli.py tensor_core` 均已实跑通过。

## 5) Vector Add 变体命名修正
- 改了什么：
  - `workloads/vector_add/vector_add_fp4.cu` 重命名为 `workloads/vector_add/vector_add_float4.cu`
  - 同步 CUDA 内核符号命名 `fp4 -> float4`
  - 更新 `update/2026-03-11.md` 中对应历史文案
- 为什么改：`fp4` 易被理解为 4-bit 浮点，和实际 `float4` 向量化实现含义不一致。
- 结果：variant 名与文件名语义统一为 `float4`。

## 6) 新增 1D ReduceSum workload
- 改了什么：
  - 新增 `workloads/reduce_sum/config.json`
  - 新增 `workloads/reduce_sum/data.py`
  - 新增 `workloads/reduce_sum/ref.py`
  - 新增 `workloads/reduce_sum/reduce_sum.cpp`
  - 新增 `workloads/reduce_sum/reduce_sum_base.cu`
- 为什么改：补充一个典型 1D reduction 原型 workload，用于快速验证 `reduce sum` 正确性和性能。
- 结果：支持直接运行 `python cli.py reduce_sum`（`mode=all`）。

## 7) 新增 CUB ReduceSum 变体
- 改了什么：新增 `workloads/reduce_sum/reduce_sum_cub.cu`，使用 `cub::DeviceReduce::Sum` 实现 1D reduce-sum。
- 为什么改：提供与手写 kernel（`base`）对照的库实现版本，便于比较性能与行为。
- 结果：`reduce_sum` workload 自动发现 `base` 与 `cub` 两个变体。

## 8) 新增 2D ReduceSum workload
- 改了什么：
  - 新增 `workloads/reduce_sum_2d/config.json`
  - 新增 `workloads/reduce_sum_2d/data.py`
  - 新增 `workloads/reduce_sum_2d/ref.py`
  - 新增 `workloads/reduce_sum_2d/reduce_sum_2d.cpp`
  - 新增 `workloads/reduce_sum_2d/reduce_sum_2d_base.cu`
- 为什么改：补充二维输入场景的 reduce-sum 原型，便于独立验证 2D 数据上的正确性与性能。
- 结果：`python cli.py reduce_sum_2d` 已在 Modal 实测通过（`1/1 passed`）。

## 9) 新增 GPU 带宽 workload
- 改了什么：
  - 新增 `workloads/gpu_bandwidth/config.json`
  - 新增 `workloads/gpu_bandwidth/data.py`
  - 新增 `workloads/gpu_bandwidth/message.py`
  - 新增 `workloads/gpu_bandwidth/gpu_bandwidth.cpp`
  - 新增 `workloads/gpu_bandwidth/gpu_bandwidth_base.cu`
- 为什么改：提供单独的显存吞吐测试入口，用于测量 copy 带宽（GB/s）。
- 结果：支持运行 `python cli.py gpu_bandwidth` 并在 Variants 显示 `GB/s`。

## 10) 新增 GPU cache 大小估算 workload
- 改了什么：
  - 新增 `workloads/cache_size/config.json`
  - 新增 `workloads/cache_size/data.py`
  - 新增 `workloads/cache_size/message.py`
  - 新增 `workloads/cache_size/cache_size.cpp`
  - 新增 `workloads/cache_size/cache_size_base.cu`
- 为什么改：提供按工作集 sweep 的缓存层级估算（L1/L2）入口。
- 结果：支持运行 `python cli.py cache_size` 并在 Variants 显示估算的 `L1~... L2~...`。
- 备注：实现采用 pointer-chasing 延迟拐点法，结果为微基准估算值。

## 11) 新增 PCIe 带宽 workload
- 改了什么：
  - 新增 `workloads/pcie_bandwidth/config.json`
  - 新增 `workloads/pcie_bandwidth/data.py`
  - 新增 `workloads/pcie_bandwidth/message.py`
  - 新增 `workloads/pcie_bandwidth/pcie_bandwidth.cpp`
  - 新增 `workloads/pcie_bandwidth/pcie_bandwidth_h2d.cu`
  - 新增 `workloads/pcie_bandwidth/pcie_bandwidth_d2h.cu`
- 为什么改：补充主机与 GPU 之间传输带宽（PCIe）测量入口，区分 H2D 和 D2H。
- 结果：支持运行 `python cli.py pcie_bandwidth` 并输出各变体 `GB/s`。

## 12) 为 reduce_sum_2d 新增 CUB 变体
- 改了什么：新增 `workloads/reduce_sum_2d/reduce_sum_2d_cub.cu`，使用 `cub::BlockReduce` 做 block-level 归约。
- 为什么改：提供 `reduce_sum_2d` 的 CUB block 原语实现，便于与手写 `base` 版本对照。
- 结果：`python cli.py reduce_sum_2d` 自动发现 `base` 和 `cub` 两个变体。

## 13) 修正 reduce_sum_2d 语义（全局和 -> 行和）
- 改了什么：
  - `workloads/reduce_sum_2d/ref.py` 改为 `x.sum(dim=1)`，输出 shape=`rows`
  - `workloads/reduce_sum_2d/data.py`/`config.json` 去除 `num_blocks` 入参
  - `workloads/reduce_sum_2d/reduce_sum_2d.cpp` 接口改为 `(x, block_size)`
  - `workloads/reduce_sum_2d/reduce_sum_2d_base.cu` 改为每个 block 处理一行
  - `workloads/reduce_sum_2d/reduce_sum_2d_cub.cu` 同步改为每行 `BlockReduce`
- 为什么改：2D reduce 预期应为 `m*n -> m`，之前错误实现成全局单值归约。
- 结果：`reduce_sum_2d` 两个变体都按行输出并参与正确性校验。

## 14) 纯命名清理（不改逻辑）
- 改了什么：
  - `workloads/gpu_bandwidth` 统一术语 `kernel_iters -> copy_iters`（保留旧字段兼容读取）
  - `workloads/pcie_bandwidth` 统一术语 `kernel_iters -> transfer_iters`（保留旧字段兼容读取）
  - 同步 C++/CUDA 内部参数命名，减少“kernel”一词在传输测试中的歧义
- 为什么改：降低沟通成本，避免“内核迭代”和“传输迭代”语义混淆。
- 结果：行为不变，旧配置可继续运行，新命名更直观。
