# 2026-03-12

## 1) 新增 Tensor Core workload
- 改了什么：
  - 新增 `workloads/tensor_core/config.json`
  - 新增 `workloads/tensor_core/data.py`
  - 新增 `workloads/tensor_core/message.py`
  - 新增 `workloads/tensor_core/tensor_core.cpp`
  - 新增 `workloads/tensor_core/tensor_core_base.cu`
- 为什么改：需要基于方案 A（WMMA）测 Tensor Core 算力（FP16 输入 + FP32 累加）。
- 结果：`python cli.py tensor_core` 可运行并输出 `ms + tflops`。

## 2) 调整 Variants 输出格式
- 改了什么：`cli.py` 中 benchmark 输出由 `custom_ms=... ms` 改为 `... ms`；`key=value` 消息改为 `value key`。
- 为什么改：统一终端展示格式，符合 `base 4.075388 ms 32.93 tflops` 的目标样式。
- 结果：Variants 行为 `base         3.723072 ms 2379.32 tflops`。

## 3) 修复 Tensor Core 构建/加载错误
- 改了什么：`tensor_core_base.cu` 用 `kWarpSize=32` 替代 host 侧 `warpSize`。
- 为什么改：修复扩展加载报错 `undefined symbol: __device_builtin_variable_warpSize`。
- 结果：Modal 实跑通过。

## 4) 目录与入口统一
- 改了什么：
  - 目录从 `worloads/` 重命名为 `workloads/`
  - `cli.py` 支持简写：`python cli.py <workload_name>`
  - 保留 `-p/--path` 兼容与旧目录 fallback
- 为什么改：简化使用方式并修正目录命名。
- 结果：`python cli.py vector_add` 与 `python cli.py tensor_core` 均已实跑通过。

## 5) Vector Add 变体命名修正
- 改了什么：
  - `workloads/vector_add/vector_add_fp4.cu` 重命名为 `workloads/vector_add/vector_add_float4.cu`
  - 同步 CUDA 内核符号命名 `fp4 -> float4`
  - 更新 `update/2026-03-11.md` 中对应历史文案
- 为什么改：`fp4` 易被理解为 4-bit 浮点，和实际 `float4` 向量化实现含义不一致。
- 结果：variant 名与文件名语义统一为 `float4`。

## 6) 新增 1D ReduceSum workload
- 改了什么：
  - 新增 `workloads/reduce_sum/config.json`
  - 新增 `workloads/reduce_sum/data.py`
  - 新增 `workloads/reduce_sum/ref.py`
  - 新增 `workloads/reduce_sum/reduce_sum.cpp`
  - 新增 `workloads/reduce_sum/reduce_sum_base.cu`
- 为什么改：补充一个典型 1D reduction 原型 workload，用于快速验证 `reduce sum` 正确性和性能。
- 结果：支持直接运行 `python cli.py reduce_sum`（`mode=all`）。

## 7) 新增 CUB ReduceSum 变体
- 改了什么：新增 `workloads/reduce_sum/reduce_sum_cub.cu`，使用 `cub::DeviceReduce::Sum` 实现 1D reduce-sum。
- 为什么改：提供与手写 kernel（`base`）对照的库实现版本，便于比较性能与行为。
- 结果：`reduce_sum` workload 自动发现 `base` 与 `cub` 两个变体。
