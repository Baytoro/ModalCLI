# 2026-04-14

## 1) 新增 mean_squared_err workload
- 改了什么：
  - 新增 `workloads/mean_squared_err/config.json`
  - 新增 `workloads/mean_squared_err/data.py`
  - 新增 `workloads/mean_squared_err/ref.py`
  - 新增 `workloads/mean_squared_err/mean_squared_err.cpp`
  - 新增 `workloads/mean_squared_err/mean_squared_err_base.cu`
  - 新增 `workloads/mean_squared_err/notes.md`
- 为什么改：实现 LeetGPU MSE 问题（N=50M 性能测试），2-pass kernel（sq errors + reduce sum）。
- 结果：待验证 `python cli.py mean_squared_err`。

## 4) 修改 mean_squared_err kernel 接口为 extern "C" solve
- 改了什么：`mean_squared_err_base.cu` 增加 `extern "C" void solve(const float*, const float*, float*, int)`，与刷题网站接口一致。
- 为什么改：适配 LeetGPU 接口规范，便于直接提交刷题。
- 结果：`solve` 为裸指针接口，`mean_squared_err_cuda` 仍通过 torch.Tensor 调用 `solve`。

## 5) 优化 mean_squared_err 为单 kernel 实现
- 改了什么：参考 LeetGPU 实现，合并为单 kernel，使用 float4 向量化 + warp shuffle 归约，移除 cudaMalloc/cudaFree。
- 为什么改：减少内存分配开销，与刷题网站实现一致。
- 结果：单 kernel 完成 sq error 计算 + block 内归约 + atomicAdd 全局归约。

## 6) 添加 .clang-format 配置
- 改了什么：新增 `.clang-format`，设置 `IndentWidth: 4`。
- 为什么改：统一代码格式为 4 空格缩进。
- 结果：所有 C++/CUDA 文件已格式化。

## 7) 更新 AGENTS.md 提交规范
- 改了什么：新增两条规则：
  - 每次提交前必须先征得用户同意
  - 每次提交前必须运行 clang-format
- 为什么改：强化提交流程规范。
- 结果：已更新 AGENTS.md。

## 2) 清理 unused workloads
- 改了什么：删除 `cache_size`, `cuda_core`, `gpu_bandwidth`, `pcie_bandwidth`, `tensor_core` 共 5 个 workload。
- 为什么改：保留核心原型 workload（vector_add, reduce_sum, reduce_sum_2d, mean_squared_err），简化仓库。
- 结果：workloads 目录仅剩 4 个 workload。

## 3) 更新 AGENTS.md
- 改了什么：在「期望形态」中增加 `notes.md` 说明（问题定义、优化日志、实验记录）。
- 为什么改：明确每个 workload 应包含 notes.md 用于记录问题背景和演进。
- 结果：文档与代码结构一致。
